import { ElementRef, Renderer } from '@angular/core';
import { NglButton } from '../buttons/button';
import { NglButtonIcon } from '../buttons/button-icon';
export declare type NglIconCategory = 'action' | 'custom' | 'doctype' | 'standard' | 'utility';
export declare class NglIcon {
    element: ElementRef;
    renderer: Renderer;
    private state;
    _icon: string;
    setIcon: string;
    readonly icon: string;
    setCategory: NglIconCategory;
    type: 'default' | 'warning' | 'error';
    align: 'left' | 'right';
    size: 'x-small' | 'small' | 'large';
    alt: string;
    svgClass: string | string[];
    private category;
    private button;
    private _containerClass;
    constructor(element: ElementRef, renderer: Renderer, state: string, button: string, nglButton: NglButton, nglButtonIcon: NglButtonIcon);
    ngOnChanges(): void;
    svgClasses(): string[];
    private readonly containerClass;
}
